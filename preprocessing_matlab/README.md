# MATLAB preprocessing source

These `.m` files are non-data source code from the historical/semi-manual preprocessing stage, reported as MATLAB R2025b. Raw recordings, detected events, paired-beat outputs, and manual-review artifacts are excluded.

The functions are retained for method audit. They are not a fully public executable pipeline because required physiological recordings and manual event-review decisions are controlled.
